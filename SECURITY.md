# Security

TruthCore uses fail-closed defaults. Retrieved text is treated as data, not instruction. Unsupported factual claims are withheld in VERIFIED_ONLY mode. Future state-changing and privileged Android actions must pass explicit policy and approval gates before execution.

Do not commit API keys, signing keys, tokens, private model credentials, or user data.
