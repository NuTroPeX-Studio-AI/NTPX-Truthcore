# Legacy Python reference

TruthCore v0.1-v0.4 was prototyped in Python/FastAPI. The native Android implementation ports its architectural invariants rather than embedding a Python server inside the APK. The validated v0.4 package remains the reference for behavior that has not yet been ported.
