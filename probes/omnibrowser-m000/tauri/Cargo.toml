[package]
name = "ntpx-omnibrowser-tauri-probe"
version = "0.0.0"
edition = "2024"
license = "Apache-2.0"
build = "build.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[workspace]
